# 敏感词检测系统维护方案

审查基线：`v2` 分支，提交 `7b15560be2c69b9bbe7b2b7d22dd3c2ae4c498f3`（2026-05-30）。

## 1. 结论

项目已经具备可演示的 Vue 3 + Gin 前后端和 AC 自动机、归一化、模糊/拼音匹配、可选 LLM 辅助能力，但当前更接近“功能原型”，尚不满足稳定部署和持续维护要求。首要问题不是新增功能，而是让系统能够可靠地失败、可重复地验证、可追踪地更新词库，并限制不受信任请求的资源消耗和数据外发。

## 2. 已确认的问题

### P0：发布前必须处理

1. **词库缺失会静默降级为空检测器。** `go-sensitive-checker/temp/` 被 `.gitignore` 排除，仓库未包含示例词库；`loadFiles` 对所有打开失败直接 `continue`，也未检查 `Scanner.Err()`。结果是服务可正常启动、健康检查返回成功，但可能没有加载任何词，形成危险的“假健康”。
2. **HTTP 服务缺少输入与资源边界。** `/api/detect` 没有请求体大小、文本长度、并发、请求超时和速率限制；Gin 使用默认 HTTP Server，未设置读写/空闲超时。长文本叠加全部类别、模糊、拼音与 LLM 检测，容易造成 CPU、内存或外部 API 成本放大。
3. **跨域策略过宽。** `cors.Default()` 不适合直接作为生产策略，应按部署域名白名单配置，并明确允许的方法和头。
4. **LLM 默认开启且会发送原文。** `SENSITIVE_ENABLE_LLM_ASSIST` 默认值为 `true`，用户启用请求选项后，原始文本会传给第三方 API。需要默认关闭、显式告知/同意、脱敏策略、审计日志及数据保留说明。

### P1：近期处理

1. **缺少 CI 和质量门禁。** 仓库没有 `.github/workflows`。后端测试存在，但没有自动执行；前端没有测试。
2. **前端 lint 当前失败。** `src/main.js` 使用 Vite 中不适用的 `process.env.NODE_ENV`，`src/pages/Home.vue` 违反多词组件名规则。`lint` 脚本默认带 `--fix`，不适合作为 CI 校验命令。
3. **生产包过大。** 实测构建成功，但主 JS 为 1,125.02 kB（gzip 372.50 kB），CSS 468.87 kB（gzip 67.69 kB）。Element Plus 与图标整体导入、未拆包是主要候选原因；生产构建还加载 Vue DevTools 插件。
4. **错误处理与健康检查不足。** `r.Run` 返回值被忽略；词库加载失败无结构化日志；`/ping` 只证明进程存活，不能证明词库可用。应拆分 liveness/readiness，并报告词库版本、加载数和必要依赖状态。
5. **配置和启动依赖工作目录。** 词库和模板使用相对路径，换目录启动或容器化后容易失效。应由配置指定绝对/可解析路径，并在启动时验证。
6. **模块结构混乱。** 根目录和后端子目录存在同名 Go module，根 `go.sum` 为空；这会干扰 IDE、工具和 CI 的模块发现。应保留单一后端 module，或明确使用 `go.work`。
7. **数据模型和统计语义含糊。** `SimilarSensitive` 固定为 `true`；响应保留 `[]any` 兼容字段；“命中词数、发生次数、方法命中次数”并存但前端仍需自行去重。应定义稳定 API schema 和统计口径。
8. **算法正确性缺少系统性验证。** 现有测试集中于增强匹配和 LLM 客户端，缺少 AC 自动机边界、Unicode/emoji、重叠命中、归一化位置映射、超长输入、重复词、空/坏词库和模糊匹配误报测试，也没有基准测试与误报/漏报评测集。

### P2：持续改进

1. **前端存在脚手架/备份残留。** `highlight_backup.vue`、未使用 views/store、两套大体量 Gin HTML 模板增加理解成本，应确认引用后删除或归档。
2. **前端架构偏单页大组件。** 展示、请求、统计口径和打码逻辑耦合，缺少 API client 层、统一错误模型和类型约束。建议逐步采用 TypeScript 或至少用 JSDoc + schema 校验。
3. **词库缺少治理机制。** 没有版本、来源、授权、审核人、生效/失效日期、变更记录、回滚、重复/冲突检查，也没有灰度发布和效果指标。
4. **缺少运维能力。** 无 Dockerfile、部署清单、结构化日志、指标、trace、告警、备份恢复说明和容量基线。
5. **文档与实现有漂移风险。** README 手工描述大量响应字段和启动步骤，但没有 OpenAPI 或契约测试；建议由 schema 生成接口文档。

## 3. 分阶段维护计划

### 第 0 阶段：建立基线（1～2 天）

- 固定 Node/Go 版本，清理重复 Go module，明确 npm 为唯一前端包管理器。
- 新增 CI：前端 `npm ci`、lint（只检查）、build、单元测试；后端 `go test -race ./...`、`go vet ./...`，并增加依赖与密钥扫描。
- 建立 `CHANGELOG.md`、Issue/PR 模板、CODEOWNERS 和发布检查表。
- 输出首份基准：典型文本的延迟、内存、误报率、漏报率、构建体积。

验收：干净环境一条命令可构建；CI 全绿；任何 lint/test 失败阻止合并。

### 第 1 阶段：消除高风险缺陷（3～5 天）

- 将词库加载改为返回错误与加载报告；必要分类为 0 词时 readiness 失败并拒绝上线；检查文件扫描错误。
- 增加请求体和文本长度上限、HTTP Server 超时、并发闸门、速率限制、优雅停机；将请求 context 传入 LLM 调用。
- 生产 CORS 使用环境化白名单；统一安全响应头；不向客户端泄露第三方底层错误。
- LLM 默认关闭。启用时要求管理员配置和前端明确提示；定义文本脱敏、日志脱敏、超时、重试和熔断策略。
- 修复现有 ESLint 错误，增加前端错误态和超时提示。

验收：空/坏词库无法通过 readiness；超限请求返回 413/422；慢请求可取消；未配置白名单的跨域请求被拒绝；默认不向第三方发送文本。

### 第 2 阶段：正确性与词库治理（1～2 周）

- 为核心算法增加表驱动测试、fuzz test、race test 和 benchmark；覆盖 Unicode、重叠、位置映射与所有匹配开关组合。
- 建立脱敏的金标准评测集，分别统计各类别 precision、recall、F1；高风险类别单独设阈值。
- 词库采用有 schema 的清单：词条 ID、文本、类别、级别、来源/授权、状态、版本、审核信息；构建时做 UTF-8、空行、重复、冲突和超长词检查。
- 词库制品独立版本化并校验 checksum；支持加载前验证、原子热更新、失败回滚和版本可观测。
- 冻结 API v1 契约，生成 OpenAPI；明确 rune 下标、区间开闭、去重规则和各统计字段含义。

验收：核心包覆盖率建议不低于 80%；每次词库变更自动生成差异及评测报告；指标下降超过阈值不得发布。

### 第 3 阶段：性能、前端和部署（1 周）

- Element Plus 与图标按需导入，生产关闭 DevTools，路由/大组件按需加载并配置 chunk 拆分。
- 用 benchmark 和 p95/p99 数据决定是否缓存、池化或调整索引；为文本长度 × 类别数 × 词库规模建立容量曲线。
- 清理未引用脚手架与旧模板，拆出 API client、检测状态和结果展示模块。
- 增加多阶段 Docker 构建、非 root 运行、只读文件系统、健康检查和资源限制；补充反向代理/TLS 部署说明。
- 增加 JSON 日志和 Prometheus 指标：请求量、延迟、错误率、限流次数、词库版本/词数、LLM 调用/失败/耗时（禁止记录原文）。

验收：设立明确包体预算（建议首屏 JS gzip < 250 kB）；目标硬件与标准数据集上的 p95 延迟和内存满足已批准 SLO；部署可回滚。

## 4. 长期维护节奏

- **每次提交：** lint、单测、race、构建、依赖/密钥扫描。
- **每周：** 依赖告警分诊、错误率和性能趋势检查、失败样本脱敏复盘。
- **每月：** 安全补丁窗口、词库质量报告、容量与成本报告、恢复演练抽查。
- **每季度：** 依赖大版本评估、威胁建模、SLO/评测阈值复审、词库授权与数据合规复核。

## 5. 建议维护指标

| 维度 | 指标 |
| --- | --- |
| 可用性 | API 成功率、readiness 失败次数、回滚次数 |
| 性能 | p50/p95/p99 延迟、峰值 RSS、每字符处理耗时 |
| 正确性 | 各类别 precision/recall/F1、位置映射准确率 |
| 词库 | 当前版本、有效词数、重复/冲突数、变更审核时长 |
| LLM | 调用率、超时率、错误率、单请求成本；不得采集原文 |
| 工程 | CI 通过率、覆盖率、依赖漏洞修复时长、前端包体 |

## 6. 本次验证记录

- 仓库成功拉取，默认分支为 `v2`。
- 前端生产构建成功，但出现大 chunk 警告；主 JS 1,125.02 kB，CSS 468.87 kB。
- ESLint 失败 2 项：`src/main.js` 的 `process` 未定义；`src/pages/Home.vue` 单词组件名。
- 前端未发现测试文件；仓库未发现 GitHub Actions 配置。
- 生产依赖审计未报告已知漏洞（使用当前解析依赖复核；正式 CI 应以 `package-lock.json` + `npm ci` 为准）。
- 当前执行环境没有 Go 工具链，因此未执行 `go test`/`go vet`；应在 CI 的固定 Go 1.23 环境补验，不能视为已通过。

## 7. 第一版维护升级落实情况

- 已增加词库加载状态、扫描错误检查以及 `/health/live`、`/health/ready`；空词库拒绝检测。
- 已增加请求体、文本长度、并发数、HTTP 超时与优雅停机配置。
- 已将 CORS 改为来源白名单并增加基础安全响应头。
- 已将 LLM 服务端默认值改为关闭，请求取消会传递至 LLM HTTP 调用，第三方错误正文不再返回给客户端。
- 已修复前端 ESLint 阻断项，拆分 lint 检查与自动修复命令，生产构建不再加载 Vue DevTools 插件。
- 已增加 GitHub Actions，执行前端依赖锁定安装、lint、build，以及后端 race test、vet。
- 尚未完成词库制品治理、算法金标准评测、前端按需导入和生产级分布式限流；这些保留在后续阶段。

## 8. 第二版维护升级落实情况

- 新增统一 API 错误结构、稳定错误码和端到端请求 ID。
- 新增类别去重/合法性、映射模式、映射数量和单项长度校验。
- 新增 `/api/status`，集中暴露词库状态、服务能力、请求限制和无敏感数据的进程内计数。
- 新增带 Bearer Token 保护的词库原子热重载；失败保留当前检测器，成功后无停机切换。
- 前端改用统一 API client，设置请求超时，支持主动取消检测并展示请求 ID。
- 前端展示服务就绪状态、真实词库统计、服务端限制和 LLM 可用性；未就绪时禁止提交。
- Element Plus 从全量插件注册改为组件级注册，生产主业务 JS 由约 1,125 kB 降至约 447 kB（拆包前实测）。
- 增加未知类别、错误结构、热重载失败保护、服务能力与限制的后端测试。
- CI 增加 Go 格式检查；本地 Go 1.23 验证已通过 `go test ./...` 与 `go vet ./...`。

## 9. 第三版词库治理升级落实情况

- 新增 Bearer Token 管理鉴权，未配置令牌时完全不注册词库管理接口。
- 新增词条分页搜索、新增、删除、批量导入预检与确认发布 API。
- 每次变更前自动创建词库文件快照，支持版本查询和指定版本回滚。
- 词库写入使用同目录临时文件、同步落盘和替换流程，写入后重新构建检测器并切换。
- 新增 JSONL 审计日志，记录管理动作、原因、请求 ID、来源 IP、版本及成功状态，不记录检测原文。
- 新增 `/admin` 前端管理控制台，覆盖词条、导入、版本和审计操作。
- 增加管理生命周期、鉴权和导入解析后端测试，以及统一 API 错误的前端 Node 测试。
- CI 增加前端测试与生产依赖高危漏洞审计。
- 升级 Axios、Vite、PostCSS、esbuild、Babel、js-yaml、nanoid、form-data 等依赖链并更新锁文件，完整 `npm audit` 验证为 0 漏洞。
